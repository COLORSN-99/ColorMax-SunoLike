{
  "name": "colormax",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev:web": "pnpm --filter @colormax/web dev",
    "build:web": "pnpm --filter @colormax/web build",
    "test:unit": "node --test packages/*/test/*.test.ts apps/web/test/*.test.ts"
  }
}
